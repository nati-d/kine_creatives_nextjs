import React from 'react'

const Button = ({text,customStyle,handlePress}) => {
  return (
    <button className='py-2 px-8 border-2 cursor-pointer border-black '>
      Shop Now
    </button>
  )
}

export default Button
